# Plugin inventory

Plugin binaries, OAuth sessions, caches, and local databases do not belong in this repository. Install plugins from their official marketplaces and authenticate them on each device.

## User-installed marketplace plugins

`frontend-design`, `vercel`, `netlify-skills`, `superpowers`, `context7`, `skill-creator`, `security-guidance`, `math-olympiad`, `agent-sdk-dev`, `claude-md-management`, `code-review`, `claude-code-setup`, `ralph-loop`, `learning-output-style`, `plugin-dev`, `hookify`, `feature-dev`, `playground`, `mcp-server-dev`, `explanatory-output-style`, `code-simplifier`, `pr-review-toolkit`, `commit-commands`, `code-modernization`, `cwc-makers`, `mcp-tunnels`, `project-artifact`, `supabase`, `stripe`, `spotify-ads-api`, `teamcity-cli`, `telegram`, `php-lsp`, `dash0`, `langfuse-observability`, `agentforce-adlc`, and `codex@openai-codex`.

## Codex desktop plugins enabled on this machine

`visualize`, `google-calendar`, `slack`, `documents`, `pdf`, `spreadsheets`, `presentations`, `template-creator`, `browser`, `computer-use`, `chrome`, `codex-app-tools`, and `sites`.

Codex and Claude Code support marketplace management. On a new machine, add the required marketplace first, then install only the plugins you need. Browser, document, and other bundled plugins may arrive with the Codex desktop application.

## Update rule

After installing, removing, enabling, or disabling a user-level plugin, update this inventory and the bootstrap documentation in the same change. Do not commit credentials, OAuth state, cache directories, or application configuration files.
